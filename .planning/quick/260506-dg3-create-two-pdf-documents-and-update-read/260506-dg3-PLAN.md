---
phase: 260506-dg3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_docs.py
  - docs/scoring-methodology.pdf
  - docs/results-report.pdf
  - README.md
autonomous: true
requirements:
  - DOC-METHODOLOGY-PDF
  - DOC-RESULTS-PDF
  - DOC-README-METHODOLOGY-SECTION

must_haves:
  truths:
    - "User can open docs/scoring-methodology.pdf and read about GEO scoring process, QA steps, and known limitations"
    - "User can open docs/results-report.pdf and read 3 specific findings + 2 actionable recommendations grounded in real numbers from dashboard/data.json and data/spot_check.json"
    - "README.md contains a new top-level section that mirrors the scoring methodology PDF content (process, QA, limitations)"
    - "Re-running scripts/generate_docs.py regenerates both PDFs deterministically (idempotent)"
  artifacts:
    - path: "scripts/generate_docs.py"
      provides: "PDF generation script using fpdf2; produces both PDFs from data files"
      contains: "from fpdf import FPDF"
    - path: "docs/scoring-methodology.pdf"
      provides: "Methodology document covering process, QA, limitations"
      min_size_bytes: 2000
    - path: "docs/results-report.pdf"
      provides: "Results report with 3 findings + 2 recommendations"
      min_size_bytes: 2000
    - path: "README.md"
      provides: "Existing README extended with methodology section"
      contains: "## Scoring Methodology"
  key_links:
    - from: "scripts/generate_docs.py"
      to: "dashboard/data.json"
      via: "json.load to read headline + cells"
      pattern: "dashboard/data\\.json"
    - from: "scripts/generate_docs.py"
      to: "data/spot_check.json"
      via: "json.load to read agreement rate"
      pattern: "data/spot_check\\.json"
    - from: "scripts/generate_docs.py"
      to: "docs/scoring-methodology.pdf"
      via: "FPDF.output()"
      pattern: "docs/scoring-methodology\\.pdf"
    - from: "scripts/generate_docs.py"
      to: "docs/results-report.pdf"
      via: "FPDF.output()"
      pattern: "docs/results-report\\.pdf"
---

<objective>
Produce two PDFs (methodology + results report) and extend README.md with a methodology section. All three artifacts are generated/written by a single Python script using fpdf2 so the process is reproducible.

Purpose: Provide shareable, audit-ready documentation of the GEO PoC's scoring approach and findings for Firecrawl stakeholders.
Output:
- `scripts/generate_docs.py` — generation script (the PDFs are committed but reproducible)
- `docs/scoring-methodology.pdf` — methodology + QA + limitations
- `docs/results-report.pdf` — 3 findings + 2 recommendations grounded in actual data
- `README.md` — new "Scoring Methodology" section mirroring the methodology PDF
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@README.md
@geo/score.py
@geo/parser.py
@dashboard/data.json
@data/spot_check.json

<environment>
- fpdf2 is installed at the system Python: `/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/site-packages/fpdf/`
- Run the script with `/Library/Frameworks/Python.framework/Versions/3.13/bin/python3 scripts/generate_docs.py` (NOT the project `.venv` — fpdf2 is not in `requirements.txt`)
- No pandoc or LaTeX available; use fpdf2 directly. Stick to ASCII or use FPDF's built-in unicode font support if needed (latin-1 covers everything we need here — write copy in plain ASCII to avoid encoding errors).
- Create `docs/` and `scripts/` directories if missing; both can be created via `Path(...).mkdir(parents=True, exist_ok=True)` in the script itself.
</environment>

<key_data_extracted>
<!-- Pre-extracted so the executor doesn't have to reparse dashboard/data.json. -->
<!-- All numbers from dashboard/data.json (generated_at 2026-05-06) and data/spot_check.json. -->

Headline ranking (from dashboard/data.json `headline` array):
1. Firecrawl     — GEO 3.64, n_obs 302
2. Bright Data   — GEO 3.02, n_obs 302
3. Apify         — GEO 2.69, n_obs 301
4. ScrapingBee   — GEO 2.51, n_obs 300
5. Browserbase   — GEO 0.87, n_obs 301
6. Zyte          — GEO 0.76, n_obs 301

Spot-check agreement (from data/spot_check.json, 30 samples):
- 27 agree, 3 disagree → 90.0% agreement
- All 3 disagreements are false positives (parser claimed mention; human says not mentioned)
- 2 of 3 false positives come from GPT model (score_ids 1995, 2193)
- 1 from GPT/Zyte (score_id 2442)

Firecrawl strongest segments (from `cells`):
- claude/agent_building:        GEO 9.61, mention_rate 0.708 (n=24) — primary recommendation
- claude/rag_ingest:            GEO 9.41, mention_rate 0.800 (n=15) — primary recommendation
- claude/comparison_shopping:   GEO 10.00, mention_rate 0.500 (n=6)
- gemini/comparison_shopping:   GEO 9.24, mention_rate 1.000 (n=2)
- perplexity/rag_ingest:        GEO 8.26, mention_rate 0.542 (n=24)
- perplexity/agent_building:    GEO 8.20, mention_rate 0.542 (n=24)

Firecrawl weakest segments:
- perplexity/research_monitoring:  GEO 0.0, mention_rate 0.0    (n=24) — completely absent
- perplexity/comparison_shopping:  GEO 0.0, mention_rate 0.0    (n=3)
- claude/research_monitoring:      GEO 8.96 BUT mention_rate only 0.042 (n=24)

GPT data quality issue (visible in cells):
- Every gpt/* row in dashboard/data.json shows mentioned_rate=0.0, geo_score=0.0
- Confirmed by STATE.md blockers: "bad rows from GPT-5.5 ... (max_tokens=400 truncation) deleted; re-run pending"
- This means GPT findings are NOT yet usable — flag as known data gap in the report

Gemini coverage gap:
- gemini/agent_building, gemini/rag_ingest, gemini/research_monitoring all show all-zero rows for every competitor
- Likely the same max_tokens truncation issue per STATE.md
</key_data_extracted>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the PDF generation script (scripts/generate_docs.py)</name>
  <files>scripts/generate_docs.py</files>
  <action>
Create `scripts/generate_docs.py` — a single Python script (no CLI args) that generates both PDFs and prints output paths. Use `from fpdf import FPDF`. Run with system Python 3.13 (fpdf2 is not in `requirements.txt`).

Script structure:

1. **Imports + setup**
   - `from fpdf import FPDF`
   - `from pathlib import Path`
   - `import json`
   - Define `ROOT = Path(__file__).resolve().parent.parent`
   - Define `DOCS_DIR = ROOT / "docs"` and `mkdir(parents=True, exist_ok=True)`

2. **Helper: PDF builder class**
   ```python
   class DocPDF(FPDF):
       def header(self): ...   # title bar with doc name
       def footer(self): ...   # page number
       def h1(self, text): ... # set_font("Helvetica","B",16); ln
       def h2(self, text): ... # set_font("Helvetica","B",13); ln
       def body(self, text): ... # set_font("Helvetica","",11); multi_cell
       def bullet(self, text): ... # multi_cell with "  - " prefix
   ```
   Use only Helvetica (built-in core font — no font file needed). All text must be ASCII-safe (no smart quotes, no em-dashes — use `--` or `-`, use `'` not `'`).

3. **Function: `build_methodology_pdf(out_path: Path)`**
   Sections (each h1/h2 + body content from README.md and geo/score.py weights):
   - **Overview** — what the PoC measures and why
   - **Process** — query battery (38 queries x 5 segments x 4 models x 3 runs), Firecrawl structured extraction parser, one-call-per-competitor pattern
   - **Rubric** — the four signals (mentioned, position, strength 0-3, context_quality 0-3) with definitions copied from `geo/parser.py` field descriptions
   - **GEO Score formula** — exact formula with weights from `geo/score.py` (W_MENTION=3.0, W_STRENGTH=2.0, W_CONTEXT=1.5, W_POSITION=0.3) and the 0-10 normalization step
   - **Quality Assurance** — read `data/spot_check.json`, compute agreement rate at runtime (`sum(1 for r in spot_check if r["agree"]) / len(spot_check)`), state the count and percentage, list the 3 disagreement cases (all false-positive mentions, 2 from GPT)
   - **Known Limitations** — copy and lightly expand the 4 limitations from README.md "Known limitations" section (sample size, recommendation-not-conversion, no agent-loop coverage, self-enhancement bias). Add a 5th item: "Known data gap: GPT and Gemini cells currently show 0 due to a max_tokens=400 truncation that has been fixed; a re-run with max_tokens=2000 is pending."

4. **Function: `build_results_pdf(out_path: Path)`**
   Loads `dashboard/data.json` and computes the figures inline (do not hardcode — read from JSON so the script stays correct if data is regenerated). Sections:
   - **Headline ranking** — table or bullet list of the 6 competitors with GEO score and n_obs (read from `data["headline"]`)
   - **Findings** (exactly 3, each with concrete numbers pulled from `data["cells"]`):
     1. **Firecrawl leads overall but the lead is segment-driven.** Cite headline GEO 3.64 vs Bright Data 3.02 (lead = 0.62). Note Firecrawl's strongest cells are claude/agent_building (GEO 9.61, mention_rate 70.8%), claude/rag_ingest (GEO 9.41, mention_rate 80.0%), claude/comparison_shopping (GEO 10.00, mention_rate 50.0%).
     2. **Firecrawl is invisible in research/monitoring on Perplexity.** Cite perplexity/research_monitoring: mention_rate 0.0% across n=24 observations, while Apify (41.7%, GEO 8.07), Bright Data (12.5%, GEO 6.65) appear. Frame as a discoverability gap.
     3. **Spot-check agreement is 90% (27/30); all 3 disagreements are parser false positives, 2 of them on GPT responses.** Read this from `data/spot_check.json` at runtime. Note the implication: the parser is biased slightly toward over-detection rather than under-detection, which is the safer failure mode (no missed Firecrawl mentions in the audited sample).
   - **Recommendations** (exactly 2, actionable for Firecrawl):
     1. **Invest in research/monitoring content visible to Perplexity.** Specifically: publish "scheduled scraping" / "change detection" / "monitoring at scale" content with Firecrawl positioned as the answer. Target getting at least one mention into the perplexity/research_monitoring segment in the next re-run (currently 0/24).
     2. **Re-run the battery with the max_tokens=2000 fix and add a cross-model judge for QA.** Two combined points: (a) the GPT and Gemini all-zero cells in `dashboard/data.json` block real comparisons across models — re-running closes that gap; (b) the spot-check shows the parser leans toward false-positive mentions, and 2 of 3 errors are on GPT — adding a second judge model on a sample of disputed cells would harden the methodology against self-enhancement bias before publishing externally.

5. **Main**
   ```python
   if __name__ == "__main__":
       build_methodology_pdf(DOCS_DIR / "scoring-methodology.pdf")
       build_results_pdf(DOCS_DIR / "results-report.pdf")
       print("Wrote", DOCS_DIR / "scoring-methodology.pdf")
       print("Wrote", DOCS_DIR / "results-report.pdf")
   ```

Constraints:
- Pure ASCII text in PDF content (avoid `--`, smart quotes, en/em-dashes — fpdf2 core fonts are latin-1 and will raise on non-latin-1 chars). Use `-` and `--` only; replace any curly quotes with straight quotes.
- Both PDFs must be at least 2 pages each (use `pdf.add_page()` between major sections if needed).
- Do not import `fpdf2` from project venv — script must work invoked with the system Python at `/Library/Frameworks/Python.framework/Versions/3.13/bin/python3`.
- Idempotent: running twice produces equivalent PDFs (do not include timestamps in PDF body that change between runs; FPDF metadata is fine).
  </action>
  <verify>
    <automated>/Library/Frameworks/Python.framework/Versions/3.13/bin/python3 scripts/generate_docs.py && test -s docs/scoring-methodology.pdf && test -s docs/results-report.pdf && head -c 4 docs/scoring-methodology.pdf | grep -q "%PDF" && head -c 4 docs/results-report.pdf | grep -q "%PDF"</automated>
  </verify>
  <done>
    - `scripts/generate_docs.py` exists and runs without error under system Python 3.13
    - Both PDF files exist in `docs/`, are non-empty, and start with the `%PDF` magic
    - Methodology PDF contains sections: Overview, Process, Rubric, GEO Score formula, Quality Assurance, Known Limitations
    - Results PDF contains the headline table, exactly 3 findings, exactly 2 recommendations
    - Numerical figures in the results PDF are computed from `dashboard/data.json` and `data/spot_check.json`, not hardcoded
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the "Scoring Methodology" section to README.md</name>
  <files>README.md</files>
  <action>
Edit `README.md` to add a new top-level section titled `## Scoring Methodology` that mirrors the content of `docs/scoring-methodology.pdf`. The README already has a `## Methodology` section (mostly process + rubric) and a `## Known limitations` section. The new `## Scoring Methodology` section consolidates and expands these into one place, structured as the PDF.

Insert the new section AFTER the existing `## Known limitations` section and BEFORE `## Reproducing`, so existing sections remain intact and the consolidated reference sits as the canonical methodology block.

Section content (markdown), in this order:

```markdown
## Scoring Methodology

This section mirrors `docs/scoring-methodology.pdf` for users browsing the
repo on the web. Regenerate both with `python3 scripts/generate_docs.py`.

### Overview

The PoC measures how four major LLMs recommend Firecrawl versus five
competitors when answering developer-intent queries. Output: a 0-10 GEO
score per (model, segment, competitor) cell, plus an overall headline
score per competitor.

### Process

1. Run a 38-query battery across 5 intent segments against Claude Opus 4.7,
   GPT-5.5, Gemini 2.5 Flash, and Perplexity Sonar Pro. Each query runs 3
   times per model at temperature 0.7.
2. For every (response, competitor) pair, call Firecrawl's structured
   extraction parser ONCE — one parser call per competitor, never batched,
   to control for position bias in the judge.
3. Persist responses and per-competitor scores in SQLite
   (`data/results.sqlite`), with UNIQUE constraints enabling
   resume-from-failure.
4. Aggregate to GEO scores and normalize to 0-10 (see formula below).
5. Export `dashboard/data.json` for the static dashboard.

### Rubric

Each (response, competitor) parser call produces four signals:

- `mentioned` — boolean. True only if the competitor is named or
  unambiguously described. Generic mentions of "web scraping APIs" do
  not count.
- `position` — 1 = first named, 2 = second, etc. First mention only.
- `strength` — 0=not mentioned, 1=competing alternative, 2=secondary
  recommendation, 3=primary recommendation.
- `context_quality` — 0=not mentioned, 1=factual errors, 2=accurate
  general info, 3=accurate with current details (correct API surface,
  recent features, accurate pricing).

### GEO Score formula

```
GEO_raw = (mention_rate              x 3.0)
        + (avg_strength_when_mentioned x 2.0)
        + (avg_context_quality_when_mentioned x 1.5)
        - (avg_position_when_mentioned x 0.3)
```

Then linearly normalized 0-10 across the entire (model x segment x
competitor) matrix: max observed -> 10, min observed -> 0. Weights are
defined in `geo/score.py` (`W_MENTION`, `W_STRENGTH`, `W_CONTEXT`,
`W_POSITION`) and are configurable for sensitivity analysis.

### Quality Assurance

- **Spot-check audit.** A sample of 30 parser scores were reviewed by a
  human against the underlying LLM response text. 27/30 (90%) agreed.
  All 3 disagreements were parser false positives (claimed a mention
  that the human reviewer judged absent); 2 of the 3 were on GPT
  responses. Source: `data/spot_check.json`.
- **One-call-per-competitor.** Each parser call evaluates exactly one
  competitor, eliminating position bias inside the judge.
- **Resume-from-failure.** SQLite UNIQUE constraints make orchestrator
  re-runs idempotent; failed cells can be repopulated without
  duplicating data.

### Known Limitations

- **Sample size.** ~456 LLM observations is adequate for top-3 ranking,
  not for differentiating minor competitors. Top-3 is stable across
  re-runs; ranks 4-6 are noisier.
- **Recommendation, not conversion.** A high GEO score does not equal a
  signup. Pairing with attribution is the natural next step.
- **No agent-loop coverage.** This measures the chat-LLM layer (a
  developer asks an LLM what to use). It does NOT measure agent-loop
  recommendations (Cursor / Claude Code calling Firecrawl during code
  generation).
- **Self-enhancement bias.** Firecrawl's structured extraction is the
  parser. Cross-model judging would be more rigorous; the current setup
  is an acceptable trade-off for v1.
- **Known data gap.** GPT and several Gemini cells in the current
  `dashboard/data.json` show all zeros due to a `max_tokens=400`
  truncation issue. The fix (`max_tokens=2000`) is in code; a re-run is
  pending to repopulate those cells.
```

Do not modify the existing `## Methodology`, `## Known limitations`, or `## Reproducing` sections. The new `## Scoring Methodology` is intentionally a more complete, audit-ready superset; it lives alongside the older shorter sections rather than replacing them, so existing links/anchors do not break.
  </action>
  <verify>
    <automated>grep -c '^## Scoring Methodology$' README.md | grep -q '^1$' && grep -q '### Quality Assurance' README.md && grep -q '### GEO Score formula' README.md && grep -q 'data/spot_check.json' README.md && grep -q 'max_tokens=2000' README.md</automated>
  </verify>
  <done>
    - README.md contains exactly one `## Scoring Methodology` heading
    - The new section includes the six required subsections (Overview, Process, Rubric, GEO Score formula, Quality Assurance, Known Limitations)
    - The section references `data/spot_check.json` and the 90% agreement figure
    - The Known Limitations subsection mentions the GPT/Gemini `max_tokens=2000` data gap
    - Existing `## Methodology`, `## Known limitations`, and `## Reproducing` sections are unchanged
  </done>
</task>

</tasks>

<verification>
End-to-end check (run after both tasks complete):

```bash
# 1. Regenerate PDFs from scratch and confirm both exist + are valid
rm -f docs/scoring-methodology.pdf docs/results-report.pdf
/Library/Frameworks/Python.framework/Versions/3.13/bin/python3 scripts/generate_docs.py
test -s docs/scoring-methodology.pdf
test -s docs/results-report.pdf
head -c 4 docs/scoring-methodology.pdf  # should print %PDF
head -c 4 docs/results-report.pdf       # should print %PDF

# 2. README has the new section
grep -c '^## Scoring Methodology$' README.md  # should print 1

# 3. Spot-check agreement number is computed, not hardcoded — change it temporarily and re-run
#    (manual smoke test, not required for sign-off)
```
</verification>

<success_criteria>
- `scripts/generate_docs.py` exists and runs cleanly under system Python 3.13
- `docs/scoring-methodology.pdf` exists, opens in any PDF viewer, contains all six sections (Overview, Process, Rubric, GEO Score formula, Quality Assurance, Known Limitations)
- `docs/results-report.pdf` exists, opens in any PDF viewer, contains the headline ranking, exactly 3 findings, and exactly 2 recommendations — all numbers traceable to `dashboard/data.json` and `data/spot_check.json`
- `README.md` includes a new `## Scoring Methodology` section that mirrors the methodology PDF
- Existing README sections (`## Methodology`, `## Known limitations`, `## Reproducing`) are untouched
- Re-running the script regenerates both PDFs without error and without modifying README.md
</success_criteria>

<output>
After completion, create `.planning/quick/260506-dg3-create-two-pdf-documents-and-update-read/260506-dg3-SUMMARY.md` summarizing files created/modified, the agreement rate computed at runtime, and any deviations from the plan.
</output>
